"""
utils
=====
Provides auxiliary utilities for the Blowfish cipher.

Functions
---------
split_blocks(data: bytes, block_size: int) -> list
    Splits the input data into blocks of the specified size.
join_blocks(blocks: list) -> bytes
    Joins the list of blocks into a single byte array.
"""
